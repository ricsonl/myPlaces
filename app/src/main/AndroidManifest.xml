<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="android.teste.myplaces">

    <permission
        android:name="android.teste.myplaces.permission.MAPS_RECEIVE"
        android:protectionLevel="signature"/>

    <uses-permission android:name="android.teste.myplaces.permission.MAPS_RECEIVE"/>
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="com.google.android.providers.gsf.permission.READ_GSERVICES" />
    <!--
         The following two permissions are not required to use
         Google Maps Android API v2, but are recommended.
    -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <uses-feature
        android:glEsVersion="0x00020000"
        android:required="false" />


    <application
        android:name="android.teste.myplaces.Util.MyApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <uses-library android:name="org.apache.http.legacy" android:required="false"/>

        <!-- NECESSÁRIO ADICIONAR ESSA CONFIGURAÇÃO PARA INFORMAR A
        VERSÃO DO GOOGLE PLAY SERVICES UTILIZADA PARA COMPILAR O APP -->
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <activity android:name="android.teste.myplaces.Controller.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="android.teste.myplaces.Controller.MapActivity"
            android:label="@string/activity_map_label"
            android:theme="@style/AppTheme"
            >
        </activity>
        <activity
            android:name="android.teste.myplaces.Controller.ManageActivity"
            android:label="@string/activity_manage_label"
            android:theme="@style/AppTheme"
            >
        </activity>
        <activity
            android:name="android.teste.myplaces.Controller.ReportActivity"
            android:label="@string/activity_report_label"
            android:theme="@style/AppTheme"
            >
        </activity>

        <!-- CHAVE API KEY -->
        <meta-data
            android:name="com.google.android.maps.v2.API_KEY"
            android:value="AIzaSyDpC8ZJjuX7yy3v7dQwAIU_uHIJRKR0b_U" />
        <!-- com chave de debug -->

    </application>

</manifest>